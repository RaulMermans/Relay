# Synthetic workspace scenarios

These scenarios compose labelled synthetic CSV fixtures already maintained under `fixtures/raw/`. They contain no production or client data. `scenarios.json` defines the source combinations used to verify workspace analysis without copying or drifting provider fixtures.
