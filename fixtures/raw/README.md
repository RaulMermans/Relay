# Synthetic raw CSV fixtures

Every fixture in this directory is synthetic test data. It contains no client, customer, provider-account, or production reporting data. Files are intentionally small and fixture-backed source signatures are defined in `docs/data/SOURCE_RULES.md`.
