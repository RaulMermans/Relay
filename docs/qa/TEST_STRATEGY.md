# Test strategy

Future testing progresses from unit to integration to E2E to regression fixtures. The highest-rigor areas are parsing, normalization, mapping, KPI formulas, comparison logic, change intelligence, connector equivalence, and report numbers.

Every discovered data or logic bug should become a regression fixture or test. Tooling and commands are intentionally deferred because no application scaffold exists.
